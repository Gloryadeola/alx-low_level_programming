#include<stdio.h>
/**
 * main - A program that prints alphabets in lowercase.
 */
int main(void)
{
	char alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}
	putcha('\n');
	return (0);
}
