#include<stdio.h>

/**
 * main - A program that prints alphabets in lowercase.
 * Return: 0 (success)
 */
int main(void)
{

	char alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}
	putcha('\n');
	return (0);
}
